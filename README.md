# Engineering Student Portal

Updated portal with working mobile navigation, relevant coding practice links, Academic Calendar, admin-managed holidays/events, resources, announcements, projects and admin login.

Render Build Command: `npm install`
Render Start Command: `npm start`

Environment variables: `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`.